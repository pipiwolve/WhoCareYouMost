medical:
  runtime:
    environment: local
  persistence:
    store: memory
  rag:
    vector-store:
      type: simple
    retrieval:
      strategy: vector
  report:
    planning:
      environment: local
